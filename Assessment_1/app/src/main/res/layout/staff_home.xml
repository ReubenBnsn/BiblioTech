<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E9EEFB"
    tools:context=".LoginActivity"
    tools:ignore="ExtraText"> <!-- Nice background colour! -->


    <!-- Title (Home) -->
    <TextView
        android:id="@+id/HomeTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/home"
        android:textAppearance="@style/TitleAppearance"
        android:layout_marginBottom="100dp"

        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.03"
        />



    <!-- Button 1 (Settings) -->
    <Button
        android:id="@+id/settingsButton"
        android:layout_width="240dp"
        android:layout_height="80dp"
        android:layout_marginStart="48dp"
        android:layout_marginEnd="48dp"
        android:layout_marginTop="60dp"
        android:layout_marginBottom="40dp"
        android:enabled="true"
        android:text="@string/settings"

        app:layout_constraintBottom_toTopOf="@id/manageCatalogueButton"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/HomeTitle"
        app:layout_constraintVertical_bias="0.2"
        />

    <!-- Button 2 (Manage Catalogue) -->
    <Button
        android:id="@+id/manageCatalogueButton"
        android:layout_width="240dp"
        android:layout_height="80dp"
        android:layout_marginStart="48dp"
        android:layout_marginEnd="48dp"
        android:layout_marginTop="40dp"
        android:layout_marginBottom="40dp"
        android:enabled="true"
        android:text="@string/manage_catalogue"

        app:layout_constraintBottom_toTopOf="@id/manageMembersButton"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/settingsButton"
        app:layout_constraintVertical_bias="0.4"
        />


    <!-- Button 3 (Manage Members) -->
    <Button
        android:id="@+id/manageMembersButton"
        android:layout_width="240dp"
        android:layout_height="80dp"
        android:layout_marginStart="48dp"
        android:layout_marginEnd="48dp"
        android:layout_marginTop="40dp"
        android:layout_marginBottom="40dp"
        android:enabled="true"
        android:text="@string/manage_members"

        app:layout_constraintBottom_toTopOf="@id/manageBookRequestsButton"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/manageCatalogueButton"
        app:layout_constraintVertical_bias="0.6"
    />


    <!-- Button 4 (Manage Book Requests) -->
    <Button
        android:id="@+id/manageBookRequestsButton"
        android:layout_width="240dp"
        android:layout_height="80dp"
        android:layout_marginStart="48dp"
        android:layout_marginEnd="48dp"
        android:layout_marginTop="40dp"
        android:layout_marginBottom="60dp"
        android:enabled="true"
        android:text="@string/manage_book_requests"

        app:layout_constraintBottom_toTopOf="@id/brandTag"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/manageMembersButton"
        app:layout_constraintVertical_bias="0.8"
    />

    <!-- Logo at the page bottom -->
    <TextView
        android:id="@+id/brandTag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textAppearance="@style/LogoAppearance"
        android:layout_marginBottom="15dp"
        android:layout_marginTop="100dp"

        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintVertical_bias="0.9"
        app:layout_constraintHorizontal_bias="0.5"
    />


</androidx.constraintlayout.widget.ConstraintLayout>